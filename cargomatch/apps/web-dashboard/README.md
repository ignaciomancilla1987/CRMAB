# web-dashboard

Dashboard operativo de CargoMatch. React + Vite + TypeScript + Tailwind.

```bash
npm install
npm run dev
```

Abre http://localhost:5173. El proxy envía `/api/*` al backend en `:8000`.
